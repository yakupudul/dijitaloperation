name: DOP Next Task (deprecated)

# Deprecated: use "DOP Autopilot" instead.
# This workflow is intentionally inert except for a notice.

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated notice
        run: |
          echo "DOP Next Task is deprecated. Use DOP Autopilot." >> "$GITHUB_STEP_SUMMARY"
