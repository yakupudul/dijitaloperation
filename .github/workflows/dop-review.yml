name: DOP Review (deprecated)

# Deprecated: review/fix now runs inside "DOP Autopilot".
# This workflow is intentionally inert.

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated notice
        run: |
          echo "DOP Review is deprecated. Use DOP Autopilot." >> "$GITHUB_STEP_SUMMARY"
