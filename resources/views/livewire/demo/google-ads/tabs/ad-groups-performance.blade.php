@php $isTr = app()->getLocale() === 'tr'; $currency = $professional['currency'] ?? ''; $rows = collect($professional['ad_groups'] ?? []); @endphp
<section class="mt-4 overflow-hidden rounded-xl bg-white ring-1 ring-inset ring-gray-200 dark:bg-gray-900 dark:ring-gray-800">
    <div class="border-b border-gray-100 px-4 py-3 dark:border-gray-800"><h3 class="font-semibold text-gray-900 dark:text-white">{{ $isTr ? 'Reklam grubu performansı' : 'Ad group performance' }}</h3><p class="mt-1 text-xs text-gray-500">{{ $isTr ? 'Seçili dönemde en yüksek harcamalı reklam grupları.' : 'Highest-spend ad groups in the selected period.' }}</p></div>
    <div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-gray-50 text-xs text-gray-500 dark:bg-white/[0.02]"><tr><th class="px-4 py-2 text-left">Campaign ID</th><th class="px-3 py-2 text-left">Ad Group ID</th><th class="px-3 py-2 text-right">{{ $isTr ? 'Harcama' : 'Spend' }}</th><th class="px-3 py-2 text-right">Clicks</th><th class="px-3 py-2 text-right">Conv.</th><th class="px-3 py-2 text-right">CPA</th><th class="px-4 py-2 text-right">ROAS</th></tr></thead><tbody class="divide-y divide-gray-100 dark:divide-gray-800">
        @forelse($rows as $row)<tr><td class="px-4 py-2">{{ $row['campaign_id'] ?? '—' }}</td><td class="px-3 py-2 font-medium">{{ $row['ad_group_id'] ?? '—' }}</td><td class="px-3 py-2 text-right">{{ number_format((float)($row['cost'] ?? 0),2,',','.') }} {{ $currency }}</td><td class="px-3 py-2 text-right">{{ number_format((int)($row['clicks'] ?? 0),0,',','.') }}</td><td class="px-3 py-2 text-right">{{ number_format((float)($row['conversions'] ?? 0),2,',','.') }}</td><td class="px-3 py-2 text-right">{{ is_numeric($row['cpa'] ?? null) ? number_format((float)$row['cpa'],2,',','.').' '.$currency : '—' }}</td><td class="px-4 py-2 text-right">{{ is_numeric($row['roas'] ?? null) ? number_format((float)$row['roas'],2,',','.').'x' : '—' }}</td></tr>@empty<tr><td colspan="7" class="px-4 py-8 text-center text-gray-400">{{ $isTr ? 'Reklam grubu günlük verisi yok.' : 'No ad group daily data.' }}</td></tr>@endforelse
    </tbody></table></div>
</section>
